<?xml version="1.0" encoding="UTF-8"?>
<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.scene.shape.Circle?>

<AnchorPane xmlns="http://javafx.com/javafx"
            xmlns:fx="http://javafx.com/fxml"
            fx:controller="com.example.demo1.marketfruits.ProfileController"
            styleClass="main-content"
            stylesheets="@../css/style.css"> <VBox spacing="20" alignment="TOP_CENTER" AnchorPane.topAnchor="30.0" AnchorPane.bottomAnchor="30.0" AnchorPane.leftAnchor="30.0" AnchorPane.rightAnchor="30.0">

    <Label text="👤 ইউজার প্রোফাইল" styleClass="welcome-title"/>
    <Separator/>

    <VBox alignment="CENTER" spacing="15">
        <StackPane alignment="CENTER">
            <Circle radius="70" fill="#E0E0E0"/>
            <Label fx:id="lblUsernameInitial" text="A" style="-fx-font-size: 48px; -fx-text-fill: #333333;"/>
        </StackPane>
        <Button text="ছবি পরিবর্তন করুন" onAction="#handleUploadPicture" styleClass="create-btn"/>
    </VBox>

    <VBox styleClass="section-card" spacing="15" maxWidth="600">
        <padding>
            <Insets top="20" right="20" bottom="20" left="20"/>
        </padding>
        <Label text="ব্যক্তিগত তথ্য" styleClass="section-title"/>

        <GridPane hgap="10" vgap="10">
            <columnConstraints>
                <ColumnConstraints hgrow="SOMETIMES" minWidth="100" prefWidth="150"/>
                <ColumnConstraints hgrow="SOMETIMES" minWidth="100" prefWidth="250"/>
            </columnConstraints>

            <Label text="সম্পূর্ণ নাম:" GridPane.columnIndex="0" GridPane.rowIndex="0" styleClass="stat-label"/>
            <TextField fx:id="txtName" editable="false" GridPane.columnIndex="1" GridPane.rowIndex="0" promptText="নাম"/>

            <Label text="মোবাইল:" GridPane.columnIndex="0" GridPane.rowIndex="1" styleClass="stat-label"/>
            <TextField fx:id="txtMobile" editable="false" GridPane.columnIndex="1" GridPane.rowIndex="1" promptText="মোবাইল"/>

            <Label text="ইমেইল:" GridPane.columnIndex="0" GridPane.rowIndex="2" styleClass="stat-label"/>
            <TextField fx:id="txtEmail" editable="false" GridPane.columnIndex="1" GridPane.rowIndex="2" promptText="ইমেইল"/>

            <Label text="ইউজারনেম:" GridPane.columnIndex="0" GridPane.rowIndex="3" styleClass="stat-label"/>
            <TextField fx:id="txtUsername" editable="false" GridPane.columnIndex="1" GridPane.rowIndex="3" promptText="ইউজারনেম"/>
        </GridPane>
    </VBox>

    <Region VBox.vgrow="ALWAYS"/>

    <Button text="লগআউট" onAction="#handleLogout" styleClass="warning-btn" maxWidth="200" minHeight="40"/>

</VBox>
</AnchorPane>